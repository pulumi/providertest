name: yaml_program
runtime: yaml
description: A minimal Random Pulumi YAML program.
outputs:
  name: ${username.id}
resources:
  username:
    type: random:RandomPet
    options:
      # Pin pulumi-random version
      version: 4.18.4
